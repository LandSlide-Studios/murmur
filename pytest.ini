[pytest]
testpaths = tests
# Live tests need Ollama on 127.0.0.1:11434. Opt IN with `pytest -m live`,
# so a plain `pytest` is green on a machine without it.
addopts = -m "not live"
markers =
    live: requires Ollama running on 127.0.0.1:11434
